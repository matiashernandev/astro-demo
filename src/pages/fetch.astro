---
//top level await
const response = await fetch("https://jsonplaceholder.typicode.com/users");

const users = await response.json();
console.log(users);
---

<h1 class="text-5xl text-sky-400">Fetch</h1>

<div class="container text-sky-400">
    <ul>
        {users.map((user) => <li class="text-sky-400"> {user.name} </li>)}
    </ul>
</div>
