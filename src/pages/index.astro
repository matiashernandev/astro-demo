---
import Layout from "../components/Layout.astro";

const games = ["Tunic", "Hades", "Zelda"];
const games2 = null;
---

<Layout pageTitle="index">
    <h1 class="text-blue-600 text-5xl py-4">Sarasa asdfsaf</h1>

    <section>
        <ul class="flex flex-col justify-center content-center items-center">
            {games.map((game) => <li>{game}</li>)}
        </ul>

        <h3>{games2 ? games2 : "nada por aquí"}</h3>
        <h3>{games2 ?? "nullish maestro"}</h3>
    </section>
</Layout>
